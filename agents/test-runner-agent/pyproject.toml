[project]
name = "test-runner-agent"
version = "0.1.0"
description = "A2A agent that runs one allow-listed test command and reports the exit code"
requires-python = ">=3.12"
dependencies = [
    "orcha-sdk==0.1.3",
    "pytest==9.1.1",
]
